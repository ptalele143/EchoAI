1. To run the Streamlit project , you will need to firstly add the openai api key in key_openai.txt file
2. After adding the openai api key , then run the following command from project's home directory
streamlit run mainapp.py